public interface ChildInt extends ParentInt{// Defining an interface ChildInt using the interface keyword, and sub interface that extends another super interface GrandParentInt, by using the extends keyword
	public double getChildsMoney();	
}
