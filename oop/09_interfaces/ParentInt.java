public interface ParentInt extends GrandParentInt{ // Defining an interface using the interface keyword, and sub interface that extends another super interface by using the extends keyword 
	public double getParentsMoney();	
}
